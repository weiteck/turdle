// use crate::{data::answers::ANSWERS, data::words::WORDS};
use super::*;

pub mod model;

// fn get_game_word() -> Vec<char> {
//     let list: Vec<&str> = ANSWERS.lines().collect();
//     let mut rng = rand::thread_rng();
//     let idx = &rng.gen_range(0..list.len());
//     let word = list.get(*idx).expect("Could not get an answer word to start game");
//     word.chars().collect()
// }

// fn is_valid_word(word: &str) -> bool {
//     todo!();
// }
